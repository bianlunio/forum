language: go
sudo: false
go:
  - 1.9.x
  - 1.10.x
  - master

services:
  - mongodb

install: true

go_import_path: forum

script:
  - go test -cover -coverpkg=./... -coverprofile=coverage.out ./tests

after_success:
  - bash <(curl -s https://codecov.io/bash)
